import * as React from "react";
import Layout from "../components/Layout";

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>NÃO ENCONTRADO</h1>
      <p>Esta rota não existe... que triste :(</p>
    </div>
  </Layout>
);

export default NotFoundPage;
